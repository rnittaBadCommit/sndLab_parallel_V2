#ifndef SERVER_HPP
# define SERVER_HPP

namespace rnitta
{



class Server
{
	public:
		// canonical
		Server();
		Server( const Server& other );
		~Server();
		Server&	operator=( const Server& other );
	
	
};


};	// namespace rnitta

#endif
